import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#08D9D6",
        bg: "#252A34",
        accent: "#FF2E63",
        text: "#020202",
      },
    },
  },
};

export default config;
